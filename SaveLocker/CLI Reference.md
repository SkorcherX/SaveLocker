# CLI Reference

**Moved.** The CLI reference is now a Help KB article, served by the console:
`web/src/help/cli-reference.md` (Help → Reference → *Agent CLI reference*).

Edit it there — it is the single source of truth. This stub exists only so
vault links and the `CLAUDE.md` file map keep resolving.
