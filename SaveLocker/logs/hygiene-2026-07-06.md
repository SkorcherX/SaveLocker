# Hygiene Review — 2026-07-06

Full-repo hygiene review: dependency audit, commit hygiene, doc↔code consistency,
prioritized action plan. All items completed as of 2026-07-08.

Decisions locked:
- **Remove** design-handoff folders (spent prototypes, already ported).
- **Ignore** `workspace.json` / `graph.json` (keep `app.json`/`appearance.json`/`core-plugins.json` tracked).
- **Commit** the `.verify/` test scripts into the repo.

## Findings summary

**A. Dependencies** — clean. No unused NuGet/npm packages. No secrets in tracked files.

**B. Commit hygiene**
1. `.verify/` tests untracked — lost on fresh clone.
2. `workspace.json` + `graph.json` tracked — noisy per-session diffs.
3. Design handoff folders — spent one-time prototypes carrying 2×1.1 MB logos.
4. `appsettings.Development.json` gitignored — fresh clone silently loses dev storage paths.
5. Legacy `wwwroot/index.html` untracked — exists only on dev machine.
6. Minor: logo PNG duplicated 5×; `web/README.md` unmodified Vite boilerplate; unused template assets; `.dockerignore` misses docs/design bloat.

**C. Doc drift** — Architecture claimed `EnsureCreated()` and vanilla-JS dashboard (both superseded). API Reference had wrong auth model, missing endpoints. Several Future Work items listed as pending were already shipped. CLI Reference missing `log` command.

**D. Code warts**
- `MachineSavePaths` outside EF (raw SQL). → Fixed `71f83ec`.
- `POST /api/machines/register` unauthenticated key rotation. → Fixed `bf67cc3`.

## Action results

| #   | Task                                                                                         | Commit    | Status                  |
| --- | -------------------------------------------------------------------------------------------- | --------- | ----------------------- |
| 1   | Repo cleanup (design handoffs, workspace.json, unused assets)                                | `2597cf1` | ✅                       |
| 2   | Bring tests + dev config into repo                                                           | `14e3320` | ✅                       |
| 3   | Docs refresh (Architecture, API Reference, Future Work, Progress, UX Roadmap, CLI Reference) | `98d8f34` | ✅                       |
| 4a  | Fold MachineSavePaths into EF (entity + migration)                                           | `71f83ec` | ✅                       |
| 4b  | Guard machine-key rotation (require admin password for re-registration)                      | `bf67cc3` | ✅                       |
| 5a  | Server-side SQLite backup (BackupService, nightly VACUUM INTO)                               | `0015cda` | ✅                       |
| 5b  | OpenAPI + generated TS types (server↔web; agent-ui stays hand-typed)                         | `1782367` | ✅                       |
| 5c  | Background lease sweep + Docker HEALTHCHECK                                                  | `82d0f71` | ✅                       |
| 5d  | Toolchain alignment + CI (Vite 8/TS 6, oxlint, ci.yml)                                       | `ecf35b5` | ✅                       |
| 5e  | Per-game glob filters + upload size limit                                                    | —         | ☐ open — see Backlog.md |
| 5f  | Bigger swings (Linux agent, code-signing)                                                    | —         | ☐ open — see Backlog.md |
